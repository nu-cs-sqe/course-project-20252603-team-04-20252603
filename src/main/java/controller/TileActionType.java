package controller;

import model.Card;
import model.Player;
import model.Tile;

public enum TileActionType {
    NONE,
    OFFER_PURCHASE,
    PAY_RENT,
    DRAW_CARD,
    PAY_TAX,
    GO_TO_JAIL,
    BANKRUPTCY_CHECK,
    GAME_OVER

}
